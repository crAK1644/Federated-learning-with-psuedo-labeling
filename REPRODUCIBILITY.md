# Reproducibility Record

## Repository / data / paper provenance

- Git: initial commit `c57f42b` on `master`. Raw dataset and paper PDF are gitignored (not tracked; too large / copyrighted).
- Data mount: `data/` — 89 flat CSVs named `<device_id>.<family>.<attack>.csv` (e.g. `1.benign.csv`,
  `4.mirai.udp.csv`), `device_info.csv` (DeviceID → name), `data_summary.csv` (per-file row counts),
  `README.md` (UCI dataset card). Total on-disk size ~7.6 GB. **No nested directories** — this is the
  flat variant of the official N-BaIoT layout, not the ZIP/nested hierarchy; `prepare_data` discovery
  handles both but only the flat form has been observed here.
- Devices 3 (`Ennio_Doorbell`) and 7 (`Samsung_SNH_1011_N_Webcam`) have 6 classes (benign + 5 gafgyt,
  no mirai files present) → `7×11 + 2×6 = 89` CSVs, confirmed by direct listing.
- 115 named numeric feature columns confirmed via header inspection of `1.benign.csv`. Every
  device/class file has far more than 1,000 rows per `data_summary.csv`.
- Paper PDF SHA-256:
  `0e751623fe0d910804112257cbeb2881fb9d5be688637c550a2f1a4723890a8a`
  (`Semisupervised_Federated-Learning-Based_Intrusion_Detection_Method_for_Internet_of_Things .pdf`).

## Host environment (build/verification machine)

- OS: macOS (Darwin 25.5.0), Apple Silicon `arm64` (Apple M4).
- CPU: 10 cores. RAM: 16 GiB (17179869184 bytes). Free disk at build start: ~21 GiB (data dir is
  external to prepared artifacts, which are small — the mini dataset is ~89,000 rows × 115 float32
  features ≈ tens of MB, so disk is not a constraint for `artifacts/`).
- Accelerator: **MPS available, CUDA not available** (`torch.backends.mps.is_available() == True`,
  `torch.cuda.is_available() == False`). Device selection must support `cpu` / `cuda` / `mps`; CI and
  the CPU-smoke acceptance gate run with `device=cpu` for determinism (MPS float32 kernels are not
  bit-reproducible across runs in the same way CPU is), GPU-scale `paper` runs are gated on
  user-provided CUDA hardware and are not executed as part of this build.
- **The project directory's absolute path must not contain spaces.** This checkout lives under
  `.../Federated learning with psuedo labeling/...`. `flwr run`'s local-simulation SuperLink
  initializes its SQLite state via Alembic, and the installed `flwr` package builds Alembic's
  `version_locations` config value by joining path(s) with a plain space (`flwr/supercore/state/
  alembic/utils.py::build_alembic_config`); Alembic itself then *splits* that value on whitespace to
  support multiple search paths. A repo path containing spaces gets shredded into bogus fragments,
  Alembic silently discovers zero migration scripts, and the state DB ends up with only an
  `alembic_version` table — every `flwr run` then fails opaquely (`no such table: fab`/`task`/...).
  Workaround applied here: physically relocate `.venv` to a space-free path
  (`~/.venvs/ssfl`) and leave a symlink at `.venv` inside the repo — `Path(__file__).resolve()`
  follows the symlink to the real, space-free location, so every path Alembic builds from
  `flwr`'s own installed location is clean. This is an environment workaround, not a code
  change; recreating `.venv` fresh with a plain `uv sync` (which would put it back inside the
  spaced path) reintroduces the bug — re-apply the move+symlink after any `uv venv`/`uv sync`
  that recreates `.venv` from scratch.

## Dependency resolution

Resolved via `uv sync` against `requires-python = ">=3.12"`, **no hard version pins** (per user
decision: "latest installable" rather than the spec's literal `torch==2.13.0` / `flwr==1.32.1`).
Resolution on this machine, recorded here for exact reproducibility:

| Package | Resolved version |
|---|---|
| Python | 3.12 |
| torch | 2.13.0 |
| flwr (`flwr[simulation]`) | 1.32.1 |
| ray | 2.55.1 |
| pandas | 3.0.3 |
| pyarrow | 25.0.0 |
| scikit-learn | 1.9.0 |
| pydantic | 2.13.4 |
| numpy | 2.5.1 |
| pyyaml | 6.0.3 |
| matplotlib | 3.11.1 |

Coincidentally, `torch` and `flwr` resolved to exactly the versions the original spec hard-pins
(2.13.0 / 1.32.1) — no conflict between "latest installable" and the spec's literal pins on this
machine at build time. Future re-resolution is not guaranteed to match; `uv.lock` is the source of
truth going forward.

## Flower Message API surface (flwr 1.32.1) — confirmed by direct inspection

This project targets the **modern message-based API**, not the deprecated `NumPyClient`/legacy
`Client` interface. Confirmed present in the installed package:

- `flwr.app`: `Message`, `RecordDict`, `ArrayRecord`, `Array`, `ConfigRecord`, `ConfigRecordValues`,
  `MetricRecord`, `MetricRecordValues`, `Context`, `Metadata`, `Error`.
- `flwr.clientapp.ClientApp`: decorator-based handler registration —
  `@app.train()`, `@app.evaluate()`, `@app.query()`, each wrapping
  `def handler(message: Message, context: Context) -> Message`. A single `ClientApp` instance is
  used per algorithm; SSFL's two logical sub-phases (proposal, distillation) are both registered
  under `@app.train()` and dispatched internally on `message.content["config"]["phase"]` (a string
  in the shared `ConfigRecord` contract), since both are training-type tasks from the client's
  perspective and Flower's `MessageType` taxonomy does not have a third slot for "phase B".
- `flwr.serverapp.ServerApp`: `@app.main()` decorator wraps the server entrypoint.
- `flwr.serverapp.strategy.strategy.Strategy` (custom-strategy base class) — abstract methods
  `configure_train`, `aggregate_train`, `configure_evaluate`, `aggregate_evaluate`, `summary`;
  concrete `start(grid, initial_arrays, num_rounds, timeout, train_config, evaluate_config,
  evaluate_fn) -> Result`. `start()` is **overridable** (not abstract) — SSFL/FD/DS-FL override it
  directly to run two `Grid.send_and_receive()` exchanges per paper round (proposal + distillation),
  matching the spec's explicit instruction ("custom Strategy whose `start` method runs two Message-API
  exchanges per paper communication round"). FL reuses the standard single
  configure_train/aggregate_train round shape and is implemented either as a thin subclass of the
  library-provided `flwr.serverapp.strategy.FedAvg` or an equivalent custom `Strategy` — decided at
  M4 implementation time based on whether FedAvg's built-in aggregation exactly satisfies the
  sample-weighted spec without a wrapper fighting the abstraction.
- `flwr.serverapp.Grid`: `create_message`, `get_node_ids`, `pull_messages`, `push_messages`, `run`,
  `send_and_receive`, `set_run` — the send/receive primitive `Strategy.start()` overrides use directly.
- `Context(run_id, node_id, node_config, state: RecordDict, run_config, series_id=0)` — `context.state`
  is a `RecordDict`, confirming the spec's design of persisting client classifier/discriminator
  weights in `Context.state` between rounds (never placed in outgoing `Message` content for SSFL).
- `flwr.serverapp.strategy` ships built-in `FedAvg`, `FedProx`, `FedAdam`, `FedYogi`, `Krum`,
  `Bulyan`, DP-wrapped variants, etc. — available as reference/reuse candidates for the FL baseline
  and later robust-aggregation extension profiles (M12/security extensions), not required for
  SSFL/FD/DS-FL which need bespoke two-phase logic.

## Implementation assumptions register

### Paper-fidelity revision (2026-07-23)

The canonical paper profiles now require CUDA and allocate one RTX 3090 across eight concurrent
ClientApp actors (`0.125` GPU each). They checkpoint every completed round and flush attempt-scoped
server/client telemetry for every epoch, protocol phase, communication round, evaluation, payload,
and one-second GPU/system sample. Mini-batch events remain available in short diagnostic profiles
but are disabled for the full matrix. Metrics, per-class metrics, confusion matrices, and
communication ledgers are durable during a run; resume restores the latest server and client model
states without mixing attempts. Superseded client weights are pruned after each atomic checkpoint;
only the latest resumable client state is retained, while designated server milestones and the
latest/final server state remain available.

SSFL no longer sends its confidence vector: only the filtered hard-label vector crosses the wire.
Table IV reproduction uses the paper's representative-client train-uplink convention and its stated
double precision for soft labels, while `communication.parquet` separately retains actual logical
and serialized totals. FD now evaluates every personalized client on the sealed test set and reports
the highest-performing participant, as described in the paper. Figures 4-6 are scheduled for all
three scenarios in `configs/experiments.yaml`; entries 15, 23, 25, and 27 below record the final
resolved behavior.

The paper leaves the following underspecified. Each is resolved with an explicit, documented default,
exposed as configuration where a reader might reasonably want the alternative.

| # | Ambiguity | Resolution | Rationale |
|---|---|---|---|
| 1 | Batch size 80 (Table I) vs 100 (Section V-C prose) | Canonical = **80** (Table I, structural). `paper_text_batch` profile override = 100. | Table I is the more concrete, structural source; prose is more likely to be an approximation/typo. |
| 2 | CNN "number of fully connected layers" (prose vs Table I) | **Table I** governs: one 128-unit dense hidden layer before each output head. | Table I gives exact per-layer filter/kernel/stride counts and output shapes; prose is contradictory and less falsifiable. |
| 3 | Min-max scaler fitting scope (paper only says values end up in [0,1]) | Canonical `paper` profile = **`all_mini`** (fit across the complete sampled mini-dataset, all splits transformed). `private_only` implemented as a leakage-safe secondary profile. | Matches the paper's blanket "[0,1]" claim most directly; `private_only` is offered because `all_mini` technically leaks open/test feature ranges into private-only training in a real deployment. |
| 4 | DS-FL sharpening temperature | **T = 0.1**, fixed per spec instruction (not derived from paper text). | Spec-mandated default; flagged as assumption since the paper's own DS-FL comparison does not specify T. |
| 5 | FD loss composition (ground-truth CE vs teacher-CE weighting) | **Equal weighting** (unweighted sum / average of the two CE terms). | Paper does not report a coefficient; equal weighting is the simplest unbiased default. |
| 6 | Optimizer-state persistence across rounds | **Adam state reinitialized at the start of every Flower task; model weights persist** across rounds via `Context.state`. | Spec-mandated; avoids stale second-moment estimates biasing a freshly-relevant-data local step while keeping the learned representation. |
| 7 | Server classifier initialization (SSFL Phase B) | **Common seeded checkpoint at round 0**, shared by all clients' initial classifiers and the server's persistent classifier; discriminators from a separately seeded common checkpoint. | Ensures fair cross-algorithm comparison — no client/server starts from a different random basin. |
| 8 | No-vote (all-abstain) handling | Emit global label **`-1`** with `valid_mask=False`; **never fabricate a class**. | Spec-mandated; fabricating a label under all-abstain would silently corrupt distillation targets with no signal backing them. |
| 9 | Tied-vote handling | **Lowest class index wins**, matching ordinary deterministic `argmax` tie-breaking (`numpy`/`torch` default). | Spec-mandated; keeps aggregation deterministic and reproducible without an arbitrary random tiebreaker. |
| 10 | MLP / LSTM architectures (not specified by the paper at all) | **MLP**: flatten 115 → 512 → 256 → 128 → output, ReLU between hidden layers, no dropout/BN. **LSTM**: reshape as 5 timesteps × 23 features, 2 layers hidden size 128 batch-first, final hidden state → 128 → 128 → output, ReLU between dense layers. | Fixed, documented architectures chosen to be reasonably capacity-matched to the CNN rather than tuned; the paper gives no basis for any specific alternative. |
| 11 | Meaning of a "communication round" in SSFL's two-phase exchange | **One paper round = exactly two Message-API exchanges** (Phase A proposal/vote, Phase B distillation), both counted as a single logical round for round-indexed comparisons (rounds 10/50/100/150/200, Table III). | Matches the spec's explicit protocol description; keeps round indexing comparable across SSFL/FL/FD/DS-FL despite SSFL doing more wire exchanges per round. |
| 12 | Device selection determinism | The canonical `paper` profile requires **CUDA** and enables `torch.use_deterministic_algorithms(True)` plus the deterministic cuBLAS workspace configuration. `smoke` remains CPU-only; MPS is not treated as deterministic. | The paper used an RTX 3090. This machine has no CUDA device, so the GPU configuration is validated but the 200-round numerical results still require execution on the user's 3090. |
| 13 | Scenario 1/2/3 client-partition shard algorithm (paper only names the scenarios, not a concrete allocation procedure) | A single McMahan-style shard partition for **both** scenario 1 and 2: sort a device's private rows by label, cut into `clients_per_device * shards_per_client` equal contiguous shards, deterministically shuffle shard order (seeded via `numpy.random.SeedSequence([seed, device_id, scenario])`), deal `shards_per_client=2` shards per client. Scenario 1 = 3 clients/device, 6 shards (700-row classes don't divide evenly into sixths -> shards straddle class boundaries -> mild mixing). Scenario 2 = `num_classes` clients/device, `2*num_classes` shards (divides evenly into class halves -> shards are always class-pure -> after shuffle+deal most clients land on exactly 1-2 classes, materially more severe skew, confirmed visually in `artifacts/data/plots/scenario_{1,2}_allocation.png`). Scenario 3 reuses scenario 2's client-per-device count; each class's 700 rows are allocated across that device's clients via `Dirichlet(alpha=0.1)` proportions -> `multinomial` counts, redrawn deterministically (incrementing attempt salt in the `SeedSequence`) until every client has >=1 example. Per-(device,class) sampling RNG seeds use `SeedSequence([seed, device_id, label])` (entropy mixing, not plain addition) so e.g. (device=2,label=3) and (device=3,label=2) never collide. | Standard, well-known non-IID shard partitioning reused instead of inventing a bespoke scheme; parametrizing only shard/client counts per scenario keeps one algorithm auditable instead of three bespoke ones, and the resulting severity ordering (S1 mild, S2/S3 severe) matches the qualitative behavior the paper's scenario framing implies. |
| 14 | FD ground-truth vs teacher-CE loss weighting (paper describes both loss terms but not their relative weight) | **Equal weight, 1:1 sum**: `loss = CE(logits, true_label) + teacher_distribution_loss(logits, leave_self_out_target)`, restricted to private examples whose class has a valid (>=1 other contributor) leave-self-out target. See `src/ssfl/protocols/fd.py::client_distillation_step`. | No basis in the paper for any other ratio; equal weighting is the simplest assumption and keeps both signals influencing training rather than one dominating by construction. |
| 15 | FD evaluation methodology | FD has no global/server model. Every personalized client is evaluated on the common sealed test set after distillation; the server reports the highest-accuracy participating client and its macro metrics. | This preserves FD personalization while matching the paper's statement that it reports the highest-performing client. |
| 16 | Per-round/per-phase random seed derivation (needed so e.g. SSFL's classifier-training, discriminator-training, and distillation sub-phases within one round don't share RNG state, while staying deterministic given the run seed) | `_seed(base_seed, server_round, salt) = (base_seed * 1000 + server_round * 10 + salt) % 2**31`, with a distinct integer `salt` per sub-phase (e.g. 0/1/2 for classifier/discriminator/distillation). See `src/ssfl/client_app.py`. | Simple, collision-free (for realistic `server_round`/salt ranges) affine derivation; avoids re-seeding all sub-phases of a round identically, which would correlate their randomness (e.g. classifier and discriminator dropout/shuffle order becoming coupled) with no benefit. |
| 17 | CPU thread allocation under `flwr run` simulation (many client processes run concurrently on one machine; PyTorch's default is one full-core OMP thread pool *per process*) | `resolve_device` calls `torch.set_num_threads(1)` whenever it resolves to `cpu` (`src/ssfl/device.py::_cpu_device`). Discovered by `sample`-profiling a live simulation worker stuck at low wall-clock progress despite real backward-pass compute: with `max_concurrent_clients: 8` and a 10-core host, N concurrent actors each spawning a 10-thread OMP pool oversubscribes the machine by ~N×; for this model's tiny per-op tensor sizes (23 channels, length 5) the OpenMP fork/join barrier overhead dominates the actual FLOPs, so 1 thread/process is faster in aggregate than 10. | Not a correctness fix — a performance one. Left unbounded, a nominal 2-round CPU smoke run took 100+ minutes; capped at 1 thread/process it completes in ~11 minutes end to end. |
| 18 | `context.run_config` vs `pyproject.toml`'s `[tool.flwr.app.config]` (Flower populates `run_config` from **every** key in that table on **every** run, not just keys explicitly passed via `flwr run --run-config`; it also rejects a `--run-config` override for any key not already declared there — `Key 'X' is not present in the main dictionary`) | `[tool.flwr.app.config]` declares only `profile = "paper"` plus a **small fixed allowlist** of knobs genuinely meant to be picked per invocation on top of a profile: `algorithm`, `scenario`, `device`. `experiment_config_from_run_config` (`src/ssfl/config.py`) loads `configs/<profile>.yaml` as the base and layers the full `run_config` dict on top — so any *other* key baked into `pyproject.toml` would be present in `run_config` on every run (profile-selected or not) and would silently clobber that field from any non-`paper` profile YAML, since there is no way to distinguish "value came from a real CLI override" from "value came from the pyproject.toml default" once Flower has flattened them into one dict. Consequence of getting this wrong (found live): a `flwr run --run-config "profile='smoke' ..."` invocation resolved to `num_server_rounds: 200`/`local_epochs: 5` (the old baked-in paper-shaped defaults) instead of smoke's `2`/`1` — the run wasn't hung, it was correctly executing a full paper-scale job under a smoke label. | Profile-defining fields (`num-server-rounds`, `local-epochs`, `batch-size`, `run-kind`, ...) must never be added to `[tool.flwr.app.config]` — express a different combination as a new `configs/<name>.yaml` and select it via `profile=<name>` instead. `algorithm`/`scenario`/`device` are the exception because every invocation needs to state them explicitly anyway; their `pyproject.toml` defaults mirror `paper.yaml` so selecting `profile="paper"` alone still needs no other overrides. |
| 19 | Flower's `Strategy.start()` round loop threads a single `arrays` variable through **both** phases of every round: it is updated only by `aggregate_train`'s return, never by `aggregate_evaluate` (which SSFL/FD/DS-FL only ever return metrics from) — so whatever `configure_evaluate` broadcast in round N is, by construction, exactly what `configure_train` receives as its `arrays` argument in round N+1, even though the proposal/local-training phase never reads it. | `configure_train` in all three custom strategies (`strategies/ssfl.py`, `strategies/fd.py`, `strategies/dsfl.py`) now ignores the framework-supplied `arrays` argument and always sends an empty `ArrayRecord()` on the train-phase message; clients only read `config["server-round"]` there. Found via the M6 comms ledger (`comms.py`) itself: round 2's SSFL train-phase message logged 80,100 logical bytes of `global_labels`/`valid_mask` that `client_app.py::_ssfl_train` never touches. | Without this, every round beyond the first silently over-counts communication for SSFL (stale `global_labels`/`valid_mask`), FD (stale `global_sum`/`contributor_counts`), and DS-FL (stale `sharpened_targets`, the largest of the three) — inflating the very comm-vs-accuracy numbers (Table IV) these protocols are supposed to look efficient on. FL is unaffected (stock `FedAvg` legitimately needs the real model arrays every train phase). |
| 20 | `flwr run`'s `--run-config` value is parsed as **TOML**, not shell-style `key=value` — an unquoted string value (e.g. `profile=smoke`) makes the CLI reject the *entire* `--run-config` string with `The provided configuration string is in an invalid format`, even though the same syntax is accepted (and shown) in Flower's own `--help` examples for simple single-key cases. | Every string-typed override in `run_suite.py`'s generated `--run-config` (`profile`, `algorithm`, `device`) is wrapped in literal double quotes (`profile="smoke_ablation_no_discriminator"`); int-typed overrides (`scenario`) are left unquoted, since TOML would reject a quoted integer the same way. | Found live: the first real `run_suite.py` invocation against `configs/experiments_smoke.yaml` reported all 3 matrix entries as `"failed"`; the underlying `flwr` stderr (only visible by reading the suite's tee'd log, not the JSON report) named the exact parse error. Any future field added to the generated `--run-config` string must be quoted or not per its TOML type, not by habit. |
| 21 | `flwr run --federation-config` no longer accepts the `options.`-prefixed key names shown in older Flower docs/examples (e.g. `options.num-supernodes=27`) — the installed flwr 1.32.1 hard-rejects it with `Unknown simulation config field(s): options.num_supernodes`, not merely a deprecation warning (a deprecation notice about `options.` fields *is* also printed, which can read as "this still works, just discouraged" when it does not). | `run_suite.py` passes the bare key: `--federation-config "num-supernodes=<count>"`. | Second of the three failures behind the same all-3-entries-failed `run_suite.py` report as #20 — fixed in the same pass once the TOML-quoting fix alone didn't clear the error. |
| 22 | Without `--stream`, `flwr run` submits the run to the SuperLink and returns almost immediately (`Successfully started run <id>`) instead of blocking until the run finishes — easy to misread as "the run is fast" or "the run is hanging" depending on what's watched next, since nothing about the CLI's own output signals that it detached. | `run_suite.py`'s `cmd` list always includes `--stream`, which is what makes the `subprocess.run(cmd, ...)` call in `run_suite()` actually block until the simulation completes and `summary.json` exists to check. | Confirmed by direct A/B: omitting `--stream` returned control in under a second with `ps aux` showing no Ray actor processes ever spawned and the target run directory's `summary.json` mtime unchanged from a prior run hours earlier; re-running the identical command with `--stream` ran past a 600-second foreground timeout and had to continue as a background task, consistent with this repo's real ~10+ minute-per-entry CPU smoke duration. Also interacts with #21's leaked-process risk: a detached (non-`--stream`) run still spawns a SuperLink/Ray process tree in the background with no local handle to await or clean up if the invoking shell/task exits first. |
| 23 | Table IV communication convention and `C@D°` | `build_report.py` uses the paper's representative-client train-uplink convention and `paper_bytes` dtype assumptions for `C@50`/`C@75`/`C@Top-Acc`; actual logical and serialized all-message totals remain in `communication.parquet`. `C@D°` has no reproduced value because simulation clients read the open set from shared storage instead of receiving it over Flower. | This reproduces the paper's published accounting without mislabeling the much larger real federation-wide traffic, and does not invent network traffic the simulation never performs. |
| 24 | `SSFLStrategy.aggregate_train`'s `Envelope` (algorithm/scenario/round/phase/dataset-hash) is built entirely from server-side state, not from anything a client reply actually carries — so `validate_envelope`'s checks on those five fields can structurally never fail as wired; only `sender_id` authorization and `message_id` dedup provide real protection today. | Documented as a known limitation in `SECURITY.md` rather than silently left implying a stronger guarantee than exists. Not fixed this pass: closing it would require every protocol's client reply to echo the envelope fields back (extra `MetricRecord` keys) and the strategies to build `Envelope` from those instead of from local state. | Judged lower-risk than it first appears — Flower's own SuperLink already matches each round's replies to that round's own outstanding requests via `reply_to`/`message_id` before `aggregate_train` ever sees them, so the remaining exposure is a compromised SuperNode tampering with reply *content* inside an otherwise legitimately-routed message, which sender-authorization and `payload_limits.py` (added same pass) do cover. |
| 25 | Exhaustive experiment logging | Attempt-scoped server and per-client JSONL streams record every epoch, protocol phase, communication round, reply, timing, checkpoint, metric, CUDA allocator sample, and one-second `nvidia-smi` sample. `log_every_batch: true` additionally records training/prediction/evaluation mini-batches for short diagnostics; canonical paper profiles set it to `false` to bound multi-run storage. After a matrix entry has a valid `summary.json`, `run_suite` losslessly gzip-compresses its JSONL files; active/interrupted runs remain plain JSONL. Raw private rows, gradients, model tensors, and secrets are excluded. | Preserves the complete scientific/protocol audit trail while preventing redundant mini-batch records and uncompressed completed logs from dominating the full experiment matrix. Concurrent client writes stay isolated and privacy-sensitive tensor content stays out of logs. |
| 26 | Even after wiring `log_event` calls into `server_app.py`/`comms.py` (#25), a live CPU smoke run's `events.jsonl` showed every per-call field (`round`, `phase`, `num_replies`, `valid_rate`, `tie_count`, ...) missing from every `aggregate` line — only the fields bound once via `bind()` (`run_id`/`algorithm`/`scenario`) came through. `log_event` passed per-call fields as `extra={"fields": fields}`, but `_Adapter.process()` (inside `bind()`) looks for a top-level `"fields"` *kwarg* to pop and merge — it never sees anything nested inside `extra`, and then overwrites `kwargs["extra"]` entirely with just the bound fields. | Fixed in `logging_utils.py::log_event`: for a `LoggerAdapter`, call `logger.info(message, fields=fields)` (top-level kwarg, matching what `process()` actually pops) instead of `extra={"fields": fields}`. Added `tests/unit/test_logging_utils.py` plus a `logging_utils.py.__main__` self-check asserting both bound and per-call fields survive into the emitted JSON, so this exact regression fails loudly next time. | Caught by actually reading the live simulation's `events.jsonl` output rather than trusting that "the wiring compiles and the run completes" meant the log content was correct — unit tests alone (which had been written against fabricated `Message` objects, not this logging path) would not have caught it, since none existed for `logging_utils.py` before this fix. |
| 27 | Mid-run checkpoint/resume and durable ledgers | Paper profiles atomically checkpoint server and all client model state every completed round. After a new checkpoint is safely written, client history is pruned to the latest resumable state; server history retains configured milestones plus the latest/final state. Resume restores the latest committed round, creates a new attempt, discards any metric/communication rows beyond that checkpoint, and replays only the interrupted round. Ledgers flush incrementally. | Live-verified by interrupting SSFL during round 2, restoring server plus 27 clients from round 1, reproducing the same round-2 aggregate, and completing with metric rounds `[1,2]`. Truncating uncommitted communication rows was added after that live audit exposed duplicate partial-round traffic. |
| 28 | `test_identical_seed_runs_are_deterministic` (M9 integration test) **failed** on its first live run: two back-to-back `flwr run` invocations of the identical DS-FL smoke config produced different `final_centralized_metrics` (`accuracy` 0.3162 vs 0.3080, `loss` 3.339 vs 3.362). First suspect: `aggregate_mean` (DS-FL), `aggregate_class_logits` (FD), and `aggregate_soft` (SSFL no-voting ablation) all summed/stacked client uploads in `list(replies)` order — Ray/Flower reply-arrival order isn't reproducible run-to-run, and floating-point addition isn't associative. Fixed (sort by `client_id`/`sender_id` before reducing; see `src/ssfl/protocols/{dsfl,fd,ssfl}.py`, order-independence proven by `tests/protocol/test_{dsfl,fd,ssfl}.py`), but a live re-run **still failed** with a comparable-magnitude gap (accuracy 0.3325 vs 0.3158) — proving that fix, while a real and worthwhile correctness improvement, was not the (sole) cause. Root-caused via two isolation probes run outside Ray entirely: (a) a standalone script training the real CNN classifier via raw PyTorch calls, and (b) a standalone script calling the actual `client_predict_step`/`train_supervised` path against real prepared client data — both run sequentially and under 6-way concurrent OS process contention, both bit-identical (down to the last mantissa bit) on every invocation. This rules out aggregation order (already fixed separately), OpenMP/BLAS thread-count contention, round-0 model-init seeding, `make_loader`'s generator-based DataLoader seeding, and client dropout (`events.jsonl` confirms `rejected_count: 0`, `num_replies: 27` on every round of every run) as the remaining cause, since none of those differ between the probes and the real pipeline except one thing: the probes never go through Ray. The build machine is an Apple M4 (`sysctl hw.perflevel0.physicalcpu`/`perflevel1.physicalcpu` = 4 P-cores / 6 E-cores) — a heterogeneous-core CPU where Apple's Accelerate/NEON matmul kernels are known to produce different low-order-bit floating-point results between P-cores and E-cores for bit-identical single-threaded input, and nothing in this codebase (or in Ray's own default scheduling) pins worker-process CPU affinity to one core type. Ray placing a client's round-1 training task on a different core type across two otherwise-identical invocations is the remaining plausible explanation: a ~1-ULP difference at round 1, amplified 10x by DS-FL's `sharpen(T=0.1)`, compounding over round 2 into the observed 1-2 point accuracy gap. | Not fixed at the application level — CPU core-type placement is outside Python/PyTorch/Ray's exposed configuration surface without OS-level `taskset`/QoS-class pinning, which is impractical to make portable and is out of scope for a CPU *smoke* gate (the paper's real results come from the GPU `paper` profile, not this CPU path). Accepted as a documented, bounded hardware-level property instead: `test_identical_seed_runs_are_deterministic` now asserts near-equality (`accuracy`/`precision`/`recall`/`f1` within `abs=0.05`, `loss` within `rel=0.05`) rather than bit-exact equality, with a comment explaining why exact equality is not a reachable bar on this hardware. The aggregation-order fix stays in place regardless — it removes one real (if not sole) source of run-to-run noise and is a correctness property worth having independent of this test's tolerance. | Two negative (bit-identical) results from isolation probes were as informative as a positive one: proving the exact same computation is deterministic *outside* Ray but not *inside* it narrows the remaining search space to "something about how Ray schedules/places work," which a Python-level fix cannot reach. Insisting on bit-exact equality here would have meant either chasing an unfixable hardware effect indefinitely or silently downgrading `warn_only=True` nondeterministic-algorithm warnings to a rug rather than a real gate — a tolerance-based check is the honest bar for a multi-process CPU simulation on heterogeneous-core hardware. |

| 29 | Live-verifying the M12 deployment scaffold (`deployment/generate_launch_configs.py`, real `flower-superlink`/`flower-supernode` processes, not the simulation backend) surfaced two bugs no unit test caught, since neither is visible without actually spawning the processes: (a) `flower-supernode`'s `--clientappio-api-address` defaults to `0.0.0.0:9094` for every node — co-located SuperNodes (this repo's smoke topology, 3 on one machine) all tried to bind the same port, so the 2nd and 3rd SuperNode died with "Port in server address 0.0.0.0:9094 is already in use" while the 1st looked fine; (b) the generated `superlink.sh`/`supernodes.sh` embedded cert paths (`{certs_dir}/server.pem` etc.) unquoted — this repo's own directory name contains spaces ("Federated learning with psuedo labeling"), so bash word-split the path and `flower-superlink --ssl-certfile ...` failed with "unrecognized arguments" on every word after the first space. | (a) `generate_launch_configs.py` now assigns each partition a distinct `--clientappio-api-address` (base port + partition_id). (b) All generated cert-path flag values go through `shlex.quote()`. Both fixes verified live: SuperLink + 3 SuperNodes register cleanly (`Fleet.ActivateNode` x3) for both the `insecure` and `tls` profiles, plus generation smoke-tested at full scale (27-client scenario 1, 89-client scenario 2) to confirm the port-assignment scheme doesn't collide at that range. Regression-covered by `tests/deployment/test_deployment_smoke.py` (spawns the real processes) and `tests/deployment/test_generate_launch_configs.py` (fast, asserts quoting and distinct ports without spawning anything). | Both bugs were invisible to `--help` inspection or code review of the generator alone — they only appear when the generated scripts are actually executed as separate OS processes on this machine's real (space-containing) path. Matches this repo's established pattern (`REPRODUCIBILITY.md` #25/#26/#28): trust live execution over "the code looks right," especially for anything touching process orchestration, ports, or shell argument passing. |

| 30 | Attempting to live-verify the M12 `deployment/docker-compose.yml` smoke topology (SuperLink + 3 SuperNode containers) required starting a local Docker daemon (`colima start`, since no daemon was running); `docker compose build` then pulled a `python:3.12-slim` base image and resolved this project's full dependency set (torch, ray, flwr) inside it. That build filled the host disk from a healthy margin down to 141Mi free (99% full) before failing outright -- colima's Lima VM disk alone (`~/.colima/_lima`) had grown to 27GB. | Stopped and deleted the colima VM (`colima stop && colima delete --force`), which reclaimed the space (141Mi -> 2.6Gi free) since that VM was disposable infrastructure created this session, not user data. Did not retry the Docker build in this environment given the demonstrated disk risk. `deployment/Dockerfile`/`deployment/docker-compose.yml` are written (same insecure-profile topology as the live-verified native scripts) but **not live-verified** here -- same honest "configured, not executed" status this repo already gives the GPU `paper` profile, for the same reason: this build environment cannot safely support it. | This machine's available disk was not checked before starting a Docker daemon and build -- should have run `df -h` first, the same instinct this repo already applies to CPU/RAM/GPU availability before claiming a gate passed. A colima/Docker-capable environment with more headroom (or a pre-pruned Docker build cache) should be able to run `docker compose -f deployment/docker-compose.yml build && up` directly; nothing about the compose file itself is untested logic, only its resource footprint on this particular machine. |

| 31 | The Dawid-Skene hard-label aggregator (`output/pdf/dawid_skene_server_changes.pdf`) is an **extension beyond the paper**, not a reproduction target: the paper aggregates SSFL hard labels by plain majority vote, and every canonical result in this repo must keep doing so. Implementing it raised four questions the brief could not settle on paper. (a) Does replacing majority with an EM estimator perturb anything else? (b) Is the E-step numerically safe across ~27 clients? (c) Is the fitted latent class `c` still *output* class `c`? (d) Does the reply-order nondeterminism of #28 reach the estimator? | Server-only seam, no wire-format change: `ssfl_hard_aggregation` ∈ `majority` (default) / `dawid_skene_shadow` / `dawid_skene`, and `ExperimentConfig` *forces* `run_kind: extension` for the two non-majority modes so these runs can never land in a canonical paper cell. (a) Shadow mode fits the estimator and broadcasts majority anyway; `tests/unit/test_strategy_dawid_skene.py::test_shadow_broadcasts_exactly_what_majority_would` asserts bit-identical label/mask arrays *and* identical values for every metric key majority emits, and `test_dawid_skene_consumes_no_random_state` asserts the global NumPy RNG position is unmoved. (b) The E-step runs in float64 log space with log-sum-exp — direct probability products underflow to zero across 27 clients near the `1e-12` floor. The monotonicity guard compares the observed-data log-likelihood **plus** the matching Dirichlet log-prior terms (the M-step is Dirichlet-MAP, so the raw likelihood is not the quantity that must increase); a decrease beyond tolerance is a fallback, not a warning. `ABSTAIN = -1` is missing data, never a modelled class: unobserved items contribute `log(1)`, not `log(prior)`, or the objective would drift with coverage. (c) The likelihood is invariant to permuting latent classes, and the output integer is used directly as a distillation target, so a post-fit permutation check (confusion diagonal-dominance over supported `(j,c)` pairs + agreement with majority) gates acceptance. (d) The annotation matrix is built sender-sorted, same argument as #28's `aggregate_soft` fix. **Every** failure path returns all-`ABSTAIN` labels with an all-false mask, so a failed fit is structurally unbroadcastable and the round falls back to majority; the reason is reported as an integer `ds_status` (`MetricRecord` holds numbers only) in `metrics.parquet` and `events.jsonl`. | Live CPU smoke run `ssfl-s1-dawid_skene_smoke-03fd3d69bfd7dc78` confirmed the seam end-to-end (shadow broadcast == majority on real 27-client data, 8900 open items) and produced a result that no unit test would have: on real scenario-1 proposals the estimator **does not deliver a usable label set at the initial engineering settings**. Round 1 fails `not_converged` at the production cap of 100 EM iterations; round 2 converges in 84 iterations (~0.9 s) but is then rejected by the permutation check at `diagonal_fraction = 0.237` against a `0.5` floor, so both rounds fall back to majority and Stage 0 scores `ds_acc == majority_acc` exactly. The threshold was deliberately **not** lowered to make the arm pass — the check is the safeguard, and tuning a safeguard until the thing it guards succeeds is how the guard stops meaning anything. The open question it exposes is whether diagonal dominance is the right identity test *here*: scenario 1 is severely non-IID, most clients genuinely only recognize two or three classes, and a client whose modal emission for a class it never sees is some other class is a specialist, not evidence of a permutation. `support > 1.0` (an absolute posterior-mass count) counts nearly all 27x11 pairs as supported at this dataset size, which dilutes the fraction with pairs the client has no real claim on. That is a Stage 0 design question for the offline screen (`scripts/ds_stage0.py --grid`), not a live-run knob. Restricted diagnostic: the raw client-by-sample annotation matrix is off by default (`dawid_skene_save_annotations: false`), opt-in per round, and to be deleted once settings are locked. Sealed open-set labels are read **only** by the offline scorer, never by the estimator, the settings, or any fallback decision, and no label file is written into `artifacts/data` (that would change the manifest hash and therefore every `run_id`). **The diagnosis in this cell is superseded by #32: the diagonal floor was the wrong instrument, and fixing it did not rescue the arm.** |

| 32 | Resolution of #31's open question ("is diagonal dominance the right identity test here?"), reached by the offline Stage 0 screen rather than by any live run. Three findings, in the order they were forced. | **(a) The diagonal statistic is permutation-blind, so it never was the permutation detector.** `diagonal_fraction` is computed from the fit's *own* posterior; rotating every client's labels moves EM to the rotated solution, which is exactly as internally self-consistent, and the number does not move (0.6389 rotated == 0.6389 honest, asserted by `test_diagonal_fraction_alone_cannot_see_a_global_relabelling`). `majority_agreement` is the only gate anchored *outside* the fit and therefore the only real permutation detector; the diagonal statistic measures internal coherence and nothing more. The check was rewritten to say so: `permutation_min_diagonal_ratio` (0.7) compares the fit against the *majority reference's* diagonal fraction through one shared `_diagonal_fraction` helper — computing the two numbers differently would make the comparison meaningless — with an absolute `1/num_classes` chance backstop checked first, for the case where the reference is itself degenerate and the ratio would otherwise admit anything. **(b) The old 0.5 absolute floor was unreachable by correct labels.** Scoring *sealed ground truth* through the same statistic gives 0.1404 (round 1) / 0.2464 (round 2) against majority's 0.2899 / 0.3141 — i.e. the floor rejected the truth. (Caveat: sealed is scored over all 8900 open items, majority only over its valid subset, so the two columns are not perfectly matched; the conclusion survives the gap.) **(c) With the gate fixed, fits are admitted and are worse.** Mean `ds_acc - majority_acc` = **-0.10** (0.4026 -> ~0.30), and identically so across all six grid variants (pseudocounts 0.01/0.1/1.0, flat class prior, 500-iteration cap, tolerance 1e-8). Per-class round 2: class 0 0.952 -> 0.000, class 3 0.616 -> 0.007, class 6 0.554 -> 0.000, class 10 0.380 -> 0.077; only class 7 (+0.116) and class 8 (+0.053) gain; classes 2, 5, 9 stay at 0.000 in both arms. The mapping is a *partial* permutation, confirmed directly: majority 0 -> DS 9 (78%), 3 -> 9 (84%), 6 -> 1 (57%)/4 (39%), 10 -> 3 (53%)/7 (37%), while 1, 4, 7, 8 stay put. | **Root cause is a model mismatch, not a tuning failure.** Dawid-Skene assumes annotator errors are conditionally independent given the true class. Federated clients here share an architecture, a distilled global model, and overlapping data shards, so their errors are correlated *by construction* — and EM explains that correlation the only way its model allows, by inventing latent class structure that merges and rotates the real classes. This is why no hyperparameter in the grid changes the outcome, and why the arm cannot be fixed by moving a threshold. It also exposes a real hole in the safeguard that remains open: `majority_agreement` is mass-weighted, reading 0.568 on the rotated fit purely because classes 1 (n=2171) and 4 (n=2221) dominate the open set — a permutation confined to small classes cannot move it, so a per-class agreement gate is the honest fix if this line is ever revived. **Stage 0 paid for itself:** EM over the 27x8900 annotation matrix costs 0.13-0.82 s per round, so the go/no-go was settled offline in seconds instead of GPU-days, and no live active run was spent on it. **Bounded scope of this verdict:** it rests on round-1/round-2 annotations from a barely-trained model, where clients are at their most correlated (all near the shared initialization). Whether the picture changes once clients have converged is untested, which is what `configs/dawid_skene_shadow.yaml` now dumps annotations at rounds 1/10/25/50 for — a shadow run changes no broadcast byte, so that evidence is free. **That caveat is now closed by #33: the picture does not change.** |

| 33 | Closure of #32's bounded-scope caveat, by the paired 50-round GPU runs `ssfl-s1-ds50_shadow-45eb87b9e6757088` and `ssfl-s1-ds50_active-85e20ab31b262308` (both `configs/paper.yaml` + the Dawid-Skene block, scenario 1, seed 2023, RTX 3090), against the matched majority control `ssfl-s1-gpu50_baseline-469254ea52f11ea0`. The open question was whether Dawid-Skene's verdict changes once clients have converged, rather than at the round-1/2 near-initialization state #32 rests on. | **It does not.** Final accuracy: baseline (majority) **0.7335**, shadow (fit DS, broadcast majority) **0.7291**, active (broadcast DS) **0.5380** — a **-19.6 point** cost. The shadow contract held exactly: `global_labels`/`valid_mask` are bit-identical to `majority_labels`/`majority_valid_mask` at every audited round (1/10/25/50), so the DS path perturbed no broadcast; the 0.44-point shadow-vs-baseline gap is the run-to-run noise floor of #34, not a Dawid-Skene effect. Active broadcast DS labels in **29 of 50 rounds** (`ok`), falling back to majority in 21 (`not_converged` 11, `permutation_check_diagonal` 9, `permutation_check_agreement` 1); on the admitted rounds DS disagreed with majority on ~47% of valid items. The offline Stage 0 screen over the shadow annotations reproduces this independently: mean `ds_acc - majority_acc` ≤ 0 for **all six** grid variants, and the only variants scoring 0.0000 achieve it by never admitting a fit at all (4/4 fallback rounds). | **The failure is the same class merge as #32, now at a converged model, and #32's predicted safeguard hole is the exact mechanism.** At round 50 the admitted fit sends benign (n=892) -> gafgyt.scan at 100%, mirai.syn (n=692) -> gafgyt.udp at 100%, mirai.udpplain -> gafgyt.junk 64%, while gafgyt.udp (n=1799) and gafgyt.scan (n=908) stay put — so `majority_agreement` reads **0.5306** against its 0.5 floor and **passes by 0.03**, while 3 of 11 classes sit at 0.00 per-class agreement. This is precisely the mass-weighted hole #32 flagged: the gate is carried by the large classes it leaves alone. Live per-class recall matches the offline screen to within noise (class 0 0.994 -> 0.000, class 8 0.991 -> 0.000, class 10 0.919 -> 0.001; gains only class 2 0.100 -> 0.995 and class 6 0.356 -> 0.747). Destroying `benign` is disqualifying for an IDS regardless of the trade. **Verdict: no-go, and the line is not worth reviving** — the collapsed classes it was built to rescue (2, 4, 6) are already fixed for free by quantile normalization, which scores 0.9577 vs all_mini's 0.7335 on the same scenario/seed and lifts class 4 from 0.000 to 0.938, a class *no* Dawid-Skene variant ever recovered. The problem was feature scaling, not label aggregation. Restricted diagnostic per #31/DATA_CARD: the annotation dumps that produced this verdict are deleted now that the question is settled. |

| 34 | `deterministic: true` did not reach client training. `configure_determinism` was called only in `server_app.py`, so every ClientApp Ray actor — where all model math actually happens — trained with `torch.use_deterministic_algorithms(False)` and `cudnn.deterministic = False`. Separately, `server_app.py` called it *after* `RunContext.create()`, which is what snapshots `environment.json`, so the reproducibility bundle recorded `deterministic_algorithms_enabled: false` / `cudnn_deterministic: false` on every run ever made regardless of config — the record said the opposite of the intent. | `client_app.py`'s `@app.train()` and `@app.evaluate()` entry points now call `configure_determinism(exp_config.deterministic)` before `resolve_device`; `server_app.py` calls it immediately after resolving `requested_config`, before the `RunContext.create()` branch (the post-branch call stays for the resume path, where `exp_config` may not be `requested_config`). | Measured, not inferred: two same-seed GPU runs differing only in the shadow seam disagreed on **6925 of 8900** round-1 open-set vote vectors — a divergence that appears *before* the server aggregator does anything, so it can only be client-side. That is a noise floor large enough to swamp most effects worth measuring, and it silently widened every same-seed comparison in this repo made on GPU. Distinct from #28, which is an unfixable heterogeneous-core CPU effect on the M4 build machine: this one is an ordinary missing call, and it is fixed. Prior GPU runs (`gpu50_baseline`, `gpu50_quantile`, both `ds50_*`) were produced *before* this fix and carry the wider floor — which is why #33's verdict rests on a -19.6 point gap and not on a fractional one. |

## Open items carried to later milestones

- Exact `Strategy.start()` override signature usage (how `evaluate_fn`, `train_config`/
  `evaluate_config` interact with a fully custom two-exchange loop) will be finalized against real
  code in M4/M5, not just introspection — introspection here is sufficient to unblock M1–M3.
- `flwr.compat` exists in 1.32.1 (legacy `NumPyClient`/`Client` compatibility shim) — confirmed
  present but intentionally unused; this project targets the message API exclusively per spec.
